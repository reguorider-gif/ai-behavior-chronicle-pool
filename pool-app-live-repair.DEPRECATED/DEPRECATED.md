# Deprecated Directory

This directory is deprecated.

The authoritative code and data now live at the project root:

- `index.html`
- `commentary-log.html`
- `ops-v2.html`
- `api/`
- `data/pool/`

This directory is retained only as historical reference and may be removed in a future cleanup release. Do not write new runtime data here.
